#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <list>
using namespace std ;
// two options sort them alphabetically , fixate one of them and try all the combinations of the other one and check if it is possible to get the first one by doing that
int main() {
    
    int trials ;
    cin >> trials ;
    for ( int i = 0 ; i < trials ; i++ ) {
        int letters ;
        string name1 , name2 ;
        cin >> letters ;
        cin >> name1 >> name2 ;
        
    }
}